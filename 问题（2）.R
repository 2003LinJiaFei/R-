#问题4-1，你将如何创建一个包含值0、0.25、0.5、0.75和1的向量？
a <- seq(from = 0,to = 1,by = 0.25)
#问题4-2，描述两种命名向量元素的方式。
#names(x) <- c("one","two","three","four")
c(apple=1,banana=2,"kiwi fruit"=3,4)
#问题4-3，向量索引中的四种类型是什么？
x[c(1,3,5)]
x[c(-2,-4)]
x[c(TRUE,FALSE,TRUE,TRUE)]
x[c("one","two","three","four")]
#问题4-4，一个3×4×5的数组的长度是多少？
60
#问题4-5，你会用哪个操作符来执行两个矩阵的内积？
%*%
